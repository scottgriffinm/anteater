# Anteater

Monorepo for the Anteater platform. Uses pnpm workspaces + Turborepo.

## npm Publishing

When publishing a new version of `next-anteater` to npm, **always follow the guide in `tasks/npm-publish.md`**. It contains the exact publish steps, pre-flight checks, and known gotchas that will break the release if missed.
